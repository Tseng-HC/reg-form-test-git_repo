# 共用樣板設定說明

## 📋 概述

`form-template.html` 是一個共用的表單樣板，可以透過 **URL 參數** 來載入不同的配置檔案，實現一個樣板服務多個活動。

---

## 🎯 方法一：使用 URL 參數（推薦）

### 基本用法

在網址後面加上 `?config=配置檔名稱`（不含 .json）

### 範例

假設您有以下配置檔案：
```
configs/
├── event-2026-01.json    # 一月活動
├── event-2026-02.json    # 二月活動
└── event-2026-03.json    # 三月活動
```

#### 存取方式：

**一月活動：**
```
https://your-domain.com/form-template.html?config=event-2026-01
```

**二月活動：**
```
https://your-domain.com/form-template.html?config=event-2026-02
```

**三月活動：**
```
https://your-domain.com/form-template.html?config=event-2026-03
```

**預設配置（不加參數）：**
```
https://your-domain.com/form-template.html
```
→ 自動載入 `configs/default-config.json`

---

## 🔧 方法二：複製樣板檔案

如果您想要每個活動有獨立的網址，可以複製樣板檔案：

### 步驟：

1. **複製樣板檔案**
   ```
   複製 form-template.html
   重新命名為 event-2026-01.html
   ```

2. **修改配置檔路徑**
   
   在新檔案中找到這行（約第 217 行）：
   ```javascript
   const configName = urlParams.get('config') || 'default-config';
   ```
   
   改為：
   ```javascript
   const configName = urlParams.get('config') || 'event-2026-01';
   ```

3. **上傳檔案**
   ```
   ├── event-2026-01.html
   ├── event-2026-02.html
   ├── form-renderer.js
   └── configs/
       ├── event-2026-01.json
       └── event-2026-02.json
   ```

### 存取方式：

**一月活動：**
```
https://your-domain.com/event-2026-01.html
```

**二月活動：**
```
https://your-domain.com/event-2026-02.html
```

---

## 📁 完整部署範例

### 檔案結構：
```
your-website/
├── form-template.html          # 共用樣板
├── form-renderer.js            # 渲染引擎（共用）
└── configs/
    ├── default-config.json     # 預設配置
    ├── lecture-jan.json        # 一月講座
    ├── lecture-feb.json        # 二月講座
    ├── workshop-mar.json       # 三月工作坊
    └── seminar-apr.json        # 四月研討會
```

### 網址對應：

| 活動       | 網址                                     | 載入配置              |
| ---------- | ---------------------------------------- | --------------------- |
| 預設       | `form-template.html`                     | `default-config.json` |
| 一月講座   | `form-template.html?config=lecture-jan`  | `lecture-jan.json`    |
| 二月講座   | `form-template.html?config=lecture-feb`  | `lecture-feb.json`    |
| 三月工作坊 | `form-template.html?config=workshop-mar` | `workshop-mar.json`   |
| 四月研討會 | `form-template.html?config=seminar-apr`  | `seminar-apr.json`    |

---

## 🔗 在 LINE 中使用

### LIFF 網址設定

在 LINE Developers Console 設定 LIFF 端點網址時，加上 config 參數：

```
https://your-domain.com/form-template.html?config=event-2026-01
```

### 分享連結

可以直接分享帶有 config 參數的網址：

```
https://your-domain.com/form-template.html?config=lecture-jan
```

---

## 💡 最佳實踐

### 1. 配置檔命名規則

建議使用有意義的命名：
- ✅ `event-2026-01-lecture.json`
- ✅ `workshop-shoulder-pain.json`
- ✅ `seminar-202601.json`
- ❌ `config1.json`
- ❌ `test.json`

### 2. 組織配置檔

如果活動很多，可以用子資料夾：
```
configs/
├── 2026/
│   ├── 01-lecture.json
│   ├── 02-workshop.json
│   └── 03-seminar.json
└── 2027/
    ├── 01-lecture.json
    └── 02-workshop.json
```

網址：
```
form-template.html?config=2026/01-lecture
```

### 3. 版本控制

為重要活動保留歷史版本：
```
configs/
├── event-2026-01.json          # 當前版本
└── archive/
    ├── event-2026-01-v1.json   # 歷史版本
    └── event-2026-01-v2.json
```

---

## 🆚 兩種方法比較

| 項目       | 方法一：URL 參數 | 方法二：複製檔案 |
| ---------- | ---------------- | ---------------- |
| 檔案數量   | 少（1個樣板）    | 多（每活動1個）  |
| 維護難度   | 簡單 ⭐           | 中等             |
| 網址美觀度 | 中等             | 高 ⭐             |
| 更新樣板   | 只需更新1個檔案  | 需更新所有檔案   |
| 靈活度     | 高 ⭐             | 中等             |
| **推薦度** | ⭐⭐⭐⭐⭐            | ⭐⭐⭐              |

---

## ❓ 常見問題

### Q1: 如何測試配置是否正確？

在瀏覽器中直接開啟：
```
http://localhost/form-template.html?config=your-config-name
```

或使用 Live Server 等工具測試。

### Q2: 配置檔找不到怎麼辦？

檢查：
1. 配置檔是否在 `configs/` 資料夾中
2. 檔案名稱是否正確（區分大小寫）
3. 檔案副檔名是否為 `.json`
4. URL 參數中不要包含 `.json`

### Q3: 可以用中文檔名嗎？

技術上可以，但**不建議**：
- ❌ `configs/一月講座.json`
- ✅ `configs/lecture-jan.json`

### Q4: 如何設定預設配置？

不加 `config` 參數時，會自動載入 `default-config.json`：
```
form-template.html  →  載入 default-config.json
```

---

## 🎓 進階技巧

### 動態生成連結

在您的網站上可以這樣產生連結：

```html
<a href="form-template.html?config=event-2026-01">一月講座報名</a>
<a href="form-template.html?config=event-2026-02">二月工作坊報名</a>
<a href="form-template.html?config=event-2026-03">三月研討會報名</a>
```

### 使用短網址

如果網址太長，可以使用短網址服務：
```
原始: https://your-domain.com/form-template.html?config=event-2026-01-shoulder-pain-workshop
短網址: https://bit.ly/abc123
```

---

## 📝 總結

**推薦使用方法一（URL 參數）**，因為：
- ✅ 只需維護一個樣板檔案
- ✅ 更新樣板時所有活動同步更新
- ✅ 配置檔案集中管理
- ✅ 靈活度高

**範例網址：**
```
https://your-domain.com/form-template.html?config=活動配置名稱
```

就這麼簡單！🎉
